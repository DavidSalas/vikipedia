/**
 * Created by david on 5/6/14.
 */

var Promise = require("bluebird");

exports.getArticle = function(articletitle)
{

}

exports.submitQuery = function(articleTitle)
{
  //TODO clear previous chart if there is one

  // TODO get the article object(article should contain image, links, description)
  // This will be async so it should return a promise.
}